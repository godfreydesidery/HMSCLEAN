package com.otapp.hmis.engine.encounter.diagnosis.domain;

public enum DiagnosisKind {
    /** Provisional / differential during work-up. */
    WORKING,
    /** Confirmed diagnosis at the end of the consultation. */
    FINAL
}
